nodobe
======

    Usage: nodobe [options]

    Required:

    -s, --script           your automation script
    -a, --app              the adobe application to use

    Optional:

    -e, --execute          whether or not to execute immediately (defaults to true)
    -c, --config           a configuration file for your script

    Any additional arguments will also be made available to your script
